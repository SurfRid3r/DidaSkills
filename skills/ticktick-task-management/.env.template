# Dida365/TickTick Account Credentials
# 将此文件复制到 ~/.ticktick/.env
#
# macOS/Linux:
#   mkdir -p ~/.ticktick
#   cp .env.template ~/.ticktick/.env
#   nano ~/.ticktick/.env  # 编辑填写凭证
#
# Windows:
#   mkdir %USERPROFILE%\.ticktick
#   copy .env.template %USERPROFILE%\.ticktick\.env
#   notepad %USERPROFILE%\.ticktick\.env  # 编辑填写凭证

DIDA_USERNAME=your_email@example.com
DIDA_PASSWORD=your_password

# Token Storage Path (可选，默认为 ~/.ticktick/token.json)
# DIDA_TOKEN_PATH=/custom/path/token.json
